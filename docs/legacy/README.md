# Legacy source

The incomplete source snapshot formerly presented as ChitChat v0.10.25 is preserved in Git history and on the `legacy/v0.10.25` branch.

It is retained for feature discovery and historical reference. It must not be deployed as a production application.
